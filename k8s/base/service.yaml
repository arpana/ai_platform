apiVersion: v1
kind: Service
metadata:
  name: ai-platform-api
  namespace: ai-platform
spec:
  type: ClusterIP
  selector:
    app: ai-platform-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
